<h1 class="title"><%= t('.Edit') %></h1>

<%= render 'form', si_unit: @si_unit %>

<div style="padding-top: 1em">
<%= link_to t('Show'), @si_unit, class: "button is-warning" %>
<%= link_to t('Back'), si_units_path, class: "button is-warning" %>
</div>
