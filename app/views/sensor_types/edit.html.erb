<h1 class="title"><%= t('.Edit') %></h1>

<%= render 'form', sensor_type: @sensor_type %>

<div style="padding-top: 1em">
<%= link_to t('Show'), @sensor_type %> |
<%= link_to t('Back'), sensor_types_path %>
</div>
